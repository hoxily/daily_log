2013-12-16 working log

按汤军义所说，项目文件中不要包含ASCII码可见字符之外的字符。说明文档需要一个统一的目录来管理。
----
资源目录本身不应该做显示非常复杂的需要经过复杂处理的表。这么一个复杂的功能远不是写死了SQL语句，仅仅替换其中的几个变量就可以做到的。除非建一个很复杂的存储过程（但是我不会），或者建许多个资源。那还不如让资源目录提供一个数据源的ConnectString，其他的事全交给具体功能实现代码来做。不受限制么？
----
通过硬编码，终于生成了汤军义所要求的综合对比表格。
也许可以设计一个更通用性的东西来描述这种类型的表达。
