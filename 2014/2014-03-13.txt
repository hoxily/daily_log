2014-03-13 working log

todolist
0.模板配置文档说明的编写。
1.修改原先的预处理程序，换成传输服务来生成xml数据。
1.过程图展示。
6.给查询字符串增加一个timeNearest参数，用于查询最接近timeNearest的数据。
9.地面气象资料（SJK）归档历史资料入库。
10.预报评分补录参考数据JSON接口。

今天有5个不认识、没见过的人来查帐（审计？）。

之前只做过ReadOnly的资源目录。这次汤军义想要能更新与写入的资源访问接口。呃！

预报评分补录参考数据
1. 国家指导报（20点起报数据）
2. 五天预报时效数据
2.1. 每12小时的天气现象
2.2. 每12小时的风力、风向（风力编号+风向编号）
2.3. 最低、最高温度

此数据用于预报评分补录的参考，也就是默认填在输入框中的内容。
如果既要做读取也要做写入操作的话，相当于是移植实现一个B/S版的产品模板。
