工作记录2016-06-03

商品详情页：
1. 面料如果为空，则完全不显示；
2. 尺码如果为空，则完全不显示；
3. 加上颜色描述，以文字描述；

购物车页面：
1. 可购买数量不正确，总计不正确。

1. 色块与色块边框的z值相同了，导值随机互相覆盖。 并不是z轴的值的问题，而是缺少panel。加上panel就好。其实色块与色块边框不管谁覆盖谁都没关系，因为色块边框比色块大一点点，而中心透明。