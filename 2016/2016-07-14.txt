工作记录2016-07-14

给ipad与魔镜互动加入的新字段与消息回应机制，依然不能解决问题。该收不到的消息，依然收不到。最多，用户可以从iPad上看到消息回应超时的提醒。然而缺少故障自动检查的机制。

一个可能的问题，那就是windows防火墙。一个新应用程序，在第一次尝试监听端口的时候，windows会弹出防火墙提示对话框，如果选择否那么就永远收不到推送的消息了。但是windows防火墙默认是允许程序向外发起连接请求的，所以iPad端能收到广播消息。

如果不考虑安全性，那么直接把魔镜设备的防火墙关闭吧。省事。