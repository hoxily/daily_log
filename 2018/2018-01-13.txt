2018-01-13

*吃、碰、杠、飘、胡特效显示不需要带上旋转，总是垂直方向显示，仅位置移到某玩家归属区域即可。（不存在的问题，本来就是不旋转的）
*吃、碰、杠、飘、胡特效太小，需要调大，约1/3高度。（已完成）
*对家的弃牌上的弃牌指针会在第二行或更多行时隐藏掉。经检查发起是spine的mesh网格应该是与UITable里的BG重叠了，被判定为被BG挡住。可以通过设置Pointer指针的material的render queue为大于UITable下的组件的material的render queue即可。（已完成）
