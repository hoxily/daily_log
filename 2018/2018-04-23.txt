2018-04-23

TODO:
* WebsocketMessageRequest模板有问题。不需要包裹在Data字段内。（已修改）
* 把SDK相关的appkey，appid统一到配置里面，而不是硬编码在代码里。
* 1.0.3bug反馈，见在线表格。
* 研究一下ShareSDK是怎么做到任意android包名下wxapi.WXEntryActivity都能正确位于包名之下。

----

用Unity3D的animation时，里面除了可以插入关键帧指定某一时刻的数值状态外，还可以切到curves分页，控制数值的变化方式。拖动关键点的控制手柄就可以调节曲线的斜率如何变化，调出线性增长以及各种非线性增长的效果。默认是非线性。因此之前弄的假匀速转动的效果看起来很奇怪。

----

炸金花看牌已完成。

与王成商量了一下。豆豆的弃、加、比、跟按钮出现得太没有规律。还是统一为轮到该用户操作时再显示。
如果轮到用户A，那么：
* 弃牌按钮总是可用；
* 加注按钮则是看情况。根据当前底注以及单注封顶参数，推算出是否能加注；
* 比牌按钮则是看当前是第几轮，如果达到最低比牌轮数要求（默认为闷3轮），那么就显示比牌按钮；
* 跟注按钮总是可用；