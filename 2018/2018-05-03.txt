2018-05-03，星期四，多云，18~26摄氏度

TODO:
* 后台服务器消息没有考虑动画延迟，光考虑逻辑数值变化，所以比牌出结果后，如果只剩一个人存活，会立即广播本局结算结果，导致比牌动画与结算动画有重叠。即使是像牛牛那边那样，服务器自觉地延迟送消息，依然会因为客户端程序进入后台，主循环卡住，从而导致回到前台后一口气收到大量消息。（完成）

* 自动跟注按钮的2个状态UI有调整。除了光圈旋转，还有打勾。具体见设计图。
* 客户端的第四方支付。
* 把SDK相关的appkey，appid统一到配置里面，而不是硬编码在代码里。
* 1.0.3bug反馈，见在线表格。
* 研究一下ShareSDK是怎么做到任意android包名下wxapi.WXEntryActivity都能正确位于包名之下。

----

一口气买了20个包子。今天就以包子过活了。感觉包子冷了后会不好吃。

----

消息预估耗时放在哪里是一个问题。
1. 在回应消息基类里添加一个virtual方法，用来定义预估耗时，默认返回0；需要定制预估耗时的消息类，override重写此方法，返回所需要的估计值。
2. 添加一个配置表，定义各种消息对应的耗时时长；

第一种简单粗暴。在C#语言层面就能搞定。除了一个小问题，那就是有可能与服务器所使用的字段重名。嗯。等到重名时再改吧。反正Visual Studio的重命名功能很智能很强大。