2018年07月20日，星期五，天气，26~36摄氏度，杭州拱墅区

新的台风要来了。从明天开始，突然从大晴天转为大暴雨。
早上来得太早，但是又没有钥匙，所以被关在门外了。据高帮友说，只有郑贤春与丁力会带钥匙。

----
添加proto对应的Java文件生成task；
添加svn update的task；
给所有的任务添加problem matcher，执行任务时不会有二次询问了。

----

网络接口的向服务器发送消息的方法命名特色是默认为Async异步操作，且是配对的请求应答模式。如果带有Sync后缀的话，则是表示同步地发送消息，会阻塞掉主线程。如果带有Undirection字样，则是表示发送无确定回复的请求。

----
工程代码处于compiler error状态中。无法运行。心里没底。
