2019年06月27日, 星期四, GPS(30.3107, 120.0907)

使用华为运动健康App连上体脂秤后，发现并不能拉取历史记录，只有最近这一次的称量数量。

犯了一个低级错误，导致昨天ListView出现不断增多列表项的bug。关键就在于，我使用了List集合想去记录SetData时从Pool里取出的列表项，但是却漏掉了把列表项加入到List中，从而在OnRecycle方法里List集合元素个数为0，造成回收失败。

所以，在使用集合时，需要注意写入操作和读取操作是否配对。
