2020年01月08日, 星期三, GPS(30.3108, 120.0904)

找到一点击Play按钮，人模就往下移的原因了。ModelManager.Update一直在监测Input输入，即便是0的鼠标Scroll操作，也会进入模型放大缩小计算，最后因为Mathf.Clamp操作，强制限制在给定的最大、最小z值处。由于TShow时的相机设置不同，重置为ModelManager操作时的参数，显然会造成问题，也就是所看到的人模往下偏移了一段距离。
