2020年03月17日，星期二，杭州，多云阴，11~21摄氏度

关于具体C# class对象没法Format整理格式的问题。解决方案如下：

javascript虽然标记了类型，但是需要注意它的实际类型。格式化时，可以传入JToken，传出的也是JToken。然后在外部进行C#类型化。

老是收到重庆的一个公司的骚扰电话。每次的号码都会变化。ios自带的拦截黑名单不管用。如果是白名单机制，仅允许已经在通讯录里的号码，那么就没法应对外卖、滴滴等陌生电话。看了下我的华为手机，上面竟然支持含通配符的黑名单。

----

unity shader入门里说3x3的矩阵无法表示平移。那是因为坐标有3维。如果是2维的UV坐标，那么3x3的矩阵表示平移和线性变换都没问题。相当于降低了一个维度。原来如此。

我知道为什么之前写的面料旋转shader代码，不能仅仅使用mul(v.uv0, _MainTexMatrix)来变换uv坐标了，还需要额外再偏移一下。因为维度不对。

----

关于Unity端模型是否应该缩放到Unity推荐的单位米，需要好好想一下。直接的好处就是与其他组件（Cloth、Physics等）、第三方资源的整合会比较好。会影响到其他的什么呢？