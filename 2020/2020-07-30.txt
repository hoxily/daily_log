2020年07月30日，星期四，杭州

使用工程里创建好的MessageTester数据，来回切换款式详情，在Profiler里并不能看到内存泄漏。也许需要将iPad上的款式列表的数据拿来加载才能看出问题所在。

我以为是LtCache对LtGeometry缓存造成的泄漏，但是在Profiler里看，每次重新加载详情页后Mesh的内存占用确实是先降再升的，而且反复多次也并不会累积起来。

javascript里两个正的浮点数使用 % 运算符运算，这个可以对应于UnityEngine.Mathf.Repeat 方法。
